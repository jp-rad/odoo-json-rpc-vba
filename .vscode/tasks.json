{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "tools: export modules (-> src)",
            "type": "shell",
            "command": "cscript.exe",
            "args": [
                "${workspaceFolder}/tools/export_modules.vbs"
            ],
            "problemMatcher": []
        }
    ]
}